#!/usr/bin/env bash
# Sourced by ./run — do not execute directly.

description() { echo "Build the project (no-op example)"; }

help() {
  cat <<'EOF'
Usage: ./run build
EOF
}

main() { echo "building..."; }
